"use client";

import ScaleField from "@/components/ScaleField";

// Scratch route — the scale field full-screen, before it earns a switcher slot.
export default function ScaleFieldPage() {
  return <ScaleField isDev />;
}
