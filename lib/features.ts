/* ── Features held back from launch ───────────────────────────────────────────
   Search and Ask both work, and neither is finished. They are the two surfaces
   that take free text, which makes them the two that can embarrass the site in
   front of someone typing something we never tried — and they are far more
   interesting as their own launches later than as unannounced rows in the dock
   on day one.

   Off means gone, not hidden: the rail rows, ⌘K, the modal, the chat overlay
   and the /api/chat endpoint all read these. There is no dead control left
   pointing at a feature that will not answer, and no open model endpoint
   sitting on the domain with nothing in front of it.

   Turning either back on is this one line. Nothing was deleted. */
export const SHOW_SEARCH = false;
export const SHOW_ASK = false;
